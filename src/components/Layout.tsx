import { FC, ReactNode } from 'react'

const Header = () => {
  return <div>Header</div>
}

const Footer = () => {
  return <div className='py-3 text-center opacity-40'>This chatbot responses are generated by OpenAI's GPT. As GPT may occasionally produce inaccurate or misleading data, always verify AI messages when they contain critical information. </div>
}

const Layout: FC<{ children: ReactNode }>  = ({ children }) => (
  <>
    <Header />
    {children}
    <Footer />  
  </>
)

export default Layout


